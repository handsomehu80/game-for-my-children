import { Question } from '../../game/types'

// Placeholder - will be populated in later tasks when subject files are created
export const allQuestions: Question[] = []

export { getRandomQuestion } from '../../game/QuestionSelector'